# =============================================================================
# 撮るほど (toruhodo) — 環境変数テンプレート
# 使い方: cp .env.example .env.local して値を埋める
# =============================================================================

# -----------------------------------------------------------------------------
# Database (libSQL / Turso) — ローカル開発は必ず localhost
# -----------------------------------------------------------------------------
# 別ターミナルで `npm run db:dev`（turso dev --db-file local.db --port 8080）
# リモート Turso (libsql://...) はローカルでは使わない
TURSO_DATABASE_URL="http://127.0.0.1:8080"
TURSO_AUTH_TOKEN="local-dev"

# -----------------------------------------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------------------------------------
BETTER_AUTH_SECRET=
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Google OAuth（GCP）
# リダイレクト: http://localhost:3000/api/auth/callback/google
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Google Maps JavaScript API（地図タブ）+ Geocoding API（地名表示）
# GCP で「Maps JavaScript API」「Geocoding API」を有効化し、キーを発行
# 制限: HTTP リファラに http://localhost:3000/* を推奨
# サーバー地名解決のみ使う場合は GOOGLE_MAPS_API_KEY でも可（IP制限向け）
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# AI (Gemini / @google/genai) — 未設定時はモック解説を返す
# -----------------------------------------------------------------------------
GEMINI_API_KEY=
# GEMINI_MODEL="gemini-3.5-flash-lite"

# -----------------------------------------------------------------------------
# Vercel Blob — 未設定時は data URL を DB に保存（開発用）
# -----------------------------------------------------------------------------
BLOB_READ_WRITE_TOKEN=

# -----------------------------------------------------------------------------
# Billing (Stripe) — docs/_spec/BILLING_DESIGN.md §7.0 / §12
# off = 現状動作（既定）/ meter = 使用量のみ記録 / enforce = 上限を強制
# -----------------------------------------------------------------------------
BILLING_MODE="off"
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# scripts/stripe-setup.mjs の出力を設定
STRIPE_PRICE_ID_PLUS_MONTHLY=
# Vercel Cron → /api/stripe/reconcile の Bearer 認証
CRON_SECRET=

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
